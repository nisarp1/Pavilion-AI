[build]
builder = "RAILPACK"

[build.railpackConfig]
rootDirectory = "backend"

[deploy]
startCommand = "python manage.py migrate && gunicorn pavilion_gemini.wsgi --bind 0.0.0.0:$PORT --timeout 120"
healthcheckPath = "/"
